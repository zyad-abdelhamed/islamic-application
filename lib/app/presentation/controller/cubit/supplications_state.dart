part of 'supplications_cubit.dart';

@immutable
 class SupplicationsState {
  final Offset offset;
  final double opacity;

  const SupplicationsState(
      {this.offset = Offset.zero, this.opacity = 1.0});
      
}

