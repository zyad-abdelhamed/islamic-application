import 'package:flutter/material.dart';

class PrayersTimeSettings extends StatelessWidget {
  const PrayersTimeSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}